# frozen_string_literal: true

RSpec.describe CppUmlClass do
  it "has a version number" do
    expect(CppUmlClass::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
